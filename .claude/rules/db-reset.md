# DB Reset

When applying new migrations and testing on Android emulators, use the following script to reset the database and clear all emulator app caches in one step:

```bash
./scripts/db-reset-and-clear-android-emulators-cache.sh
```
