# Edge Function Creation

When creating a new Supabase Edge Function, always use the CLI to generate the boilerplate:

```bash
supabase functions new <function_name>
```

Then edit the generated files (`index.ts`, `deno.json`) from there. Do NOT create Edge Function files manually from scratch.
