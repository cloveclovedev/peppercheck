repos:
  - repo: local
    hooks:
      - id: deno-fmt
        name: Deno fmt (Edge Functions)
        entry: deno fmt --config supabase/deno.jsonc --check
        language: system
        pass_filenames: true
        files: ^supabase/functions/.*\.(ts|tsx|js|jsx)$
      - id: deno-lint
        name: Deno lint (Edge Functions)
        entry: deno lint --config supabase/deno.jsonc
        language: system
        pass_filenames: true
        files: ^supabase/functions/.*\.(ts|tsx|js|jsx)$
      - id: prettier-webapp
        name: Prettier (Webapp)
        entry: bash -c 'cd peppercheck-webapp && npx prettier --check .'
        language: system
        pass_filenames: false
        files: ^peppercheck-webapp/
      - id: dart-format
        name: Dart format (Flutter)
        entry: dart format --set-exit-if-changed --line-length 80
        language: system
        pass_filenames: true
        files: ^peppercheck_flutter/.*\.dart$
      - id: flutter-analyze
        name: Flutter analyze
        entry: bash -c 'cd peppercheck_flutter && flutter analyze --no-fatal-infos'
        language: system
        pass_filenames: false
        files: ^peppercheck_flutter/.*\.dart$
      - id: no-stripe-live-keys
        name: Detect Stripe live keys
        entry: 'sk_live_|rk_live_|whsec_live_'
        language: pygrep
        files: \.(ts|tsx|js|jsx|dart|sql|toml|json|yaml)$
        exclude: \.pre-commit-config\.yaml$
