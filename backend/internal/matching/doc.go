// Package matching owns asynchronous referee matching for published tasks:
// candidate selection, the match/sweep worker handlers, referee availability,
// and the typed matching configuration. Phase 4a introduces the schema and the
// Phase 5 point/obligation seams; the store, service, and handlers land in
// later tasks of the phase.
package matching
