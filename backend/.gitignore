# Compiled binary produced by `go build ./cmd/peppercheck`
/peppercheck
# Go test/coverage artifacts
*.test
*.out

# Throwaway self-signed TLS cert generated by scripts/pgbackrest-local-smoke.sh
# for the local MinIO smoke stack (compose.test.yaml); regenerated every run.
/deploy/test-fixtures/minio-certs/

# Placeholder secret files generated by scripts/assert-prod-config.sh so
# `docker compose -f compose.prod.yaml config` can resolve locally/in CI.
# Never real secrets -- real production secrets are rendered at deploy time
# (Tasks 6/8/10) and never touch this repo.
/secrets/
