# For detailed configuration reference documentation, visit:
# https://supabase.com/docs/guides/local-development/cli/config
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "supabase"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. `public` and `graphql_public` schemas are included by default.
schemas = ["public", "graphql_public"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[api.tls]
# Enable HTTPS endpoints locally using a self-signed certificate.
enabled = false

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 17

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Specifies when a server connection can be reused by other clients.
# Configure one of the supported pooler modes: `transaction`, `session`.
pool_mode = "transaction"
# How many server connections to allow per user/database pair.
default_pool_size = 20
# Maximum number of client connections allowed.
max_client_conn = 100

# [db.vault]
# secret_key = "env(SECRET_VALUE)"

[db.migrations]
# Specifies an ordered list of schema files that describe your database.
# Supports glob patterns relative to supabase directory: "./schemas/*.sql"
schema_paths = [
  "./schemas/extensions.sql",
# tables
  "./schemas/common/tables/currencies.sql",
  "./schemas/profile/tables/profiles.sql",
  "./schemas/stripe/tables/stripe_accounts.sql",

  # Point
  "./schemas/point/tables/point_wallets.sql",
  "./schemas/point/tables/point_ledger.sql",

  # Reward
  "./schemas/reward/tables/reward_wallets.sql",
  "./schemas/reward/tables/reward_ledger.sql",
  "./schemas/reward/tables/reward_exchange_rates.sql",
  "./schemas/reward/tables/reward_payouts.sql",

  # Task
  "./schemas/task/tables/tasks.sql",

  # Evidence
  "./schemas/evidence/tables/task_evidences.sql",
  "./schemas/evidence/tables/task_evidence_assets.sql",

  # Matching
  "./schemas/matching/tables/enums.sql",
  "./schemas/matching/tables/matching_config.sql",
  "./schemas/matching/tables/referee_available_time_slots.sql",
  "./schemas/matching/tables/task_referee_requests.sql",

  # Judgement (Moved BEFORE Rating due to FK dependency)
  "./schemas/judgement/tables/judgements.sql",
  "./schemas/judgement/tables/judgement_threads.sql",
  "./schemas/judgement/tables/judgement_thread_assets.sql",

  # Rating
  "./schemas/rating/tables/rating_histories.sql",
  "./schemas/rating/tables/user_ratings.sql",

  # Subscription
  "./schemas/subscription/tables/enums.sql",
  "./schemas/subscription/tables/subscription_plans.sql",
  "./schemas/subscription/tables/subscription_plan_prices.sql",
  "./schemas/subscription/tables/user_subscriptions.sql",

  # Notification
  "./schemas/notification/tables/user_fcm_tokens.sql",


# views
  "./schemas/judgement/views/*.sql",
  # Common
  "./schemas/common/functions/handle_updated_at.sql",

  # Profile
  "./schemas/profile/functions/auth_helpers.sql",

  # task
  # "./schemas/task/functions/*.sql", # keeping wildcard if there are others, or check first.
  
  # matching
  "./schemas/matching/functions/create_matching_request.sql",
  "./schemas/matching/functions/create_referee_available_time_slot.sql",
  "./schemas/matching/functions/get_point_for_matching_strategy.sql",
  "./schemas/matching/functions/update_referee_available_time_slot.sql",
  "./schemas/matching/functions/delete_referee_available_time_slot.sql",
  "./schemas/matching/functions/process_matching.sql",
  "./schemas/matching/functions/auto_score_timeout_referee.sql",
  "./schemas/matching/functions/detect_referee_timeouts.sql",
  "./schemas/matching/functions/get_active_referee_tasks.sql",
  
  # evidence
  "./schemas/evidence/functions/detect_evidence_timeouts.sql",
  "./schemas/evidence/functions/validate_evidence_due_date.sql",
  "./schemas/evidence/functions/submit_evidence.sql",
  
  # rating
  "./schemas/rating/functions/update_user_ratings.sql",
  
  # judgement
  "./schemas/judgement/functions/confirm_judgement_and_rate_referee.sql",
  "./schemas/judgement/functions/confirm_evidence_timeout.sql",
  "./schemas/judgement/functions/confirm_review_timeout.sql",
  "./schemas/judgement/functions/detect_review_timeouts.sql",
  "./schemas/judgement/functions/reopen_judgement.sql",
  "./schemas/judgement/functions/judge_evidence.sql",
  
  # existing wildcards (checking required)
  "./schemas/auth/functions/handle_new_user.sql",
  
  # Task extra functions
  "./schemas/task/functions/create_task.sql",
  "./schemas/task/functions/update_task.sql",
  
  # Task Utils
  "./schemas/task/functions/utils/validate_task_inputs.sql",
  "./schemas/task/functions/utils/validate_task_open_requirements.sql",
  "./schemas/task/functions/utils/create_task_referee_requests_from_json.sql",
  
  # Point
  "./schemas/point/functions/consume_points.sql",
  "./schemas/point/functions/lock_points.sql",
  "./schemas/point/functions/unlock_points.sql",
  "./schemas/point/functions/grant_subscription_points.sql",

  # Reward
  "./schemas/reward/functions/grant_reward.sql",
  "./schemas/reward/functions/deduct_reward_for_payout.sql",
  "./schemas/reward/functions/prepare_monthly_payouts.sql",

  # Notification
  "./schemas/notification/functions/notify_event.sql",

# triggers
  "./schemas/auth/triggers/on_auth_user_created.sql",
  
  # Judgement
  "./schemas/judgement/triggers/on_judgements_update_set_updated_at.sql",
  "./schemas/judgement/triggers/on_judgements_timeout_score_referee.sql",
  "./schemas/judgement/triggers/on_judgement_confirmed_close_request.sql",
  "./schemas/judgement/triggers/on_judgement_confirmed.sql",
  "./schemas/judgement/triggers/on_judgements_evidence_timeout_close_referee_request.sql",
  "./schemas/judgement/triggers/on_evidence_timeout_settle.sql",
  "./schemas/judgement/triggers/on_review_timeout_settle.sql",
  "./schemas/judgement/triggers/on_judgement_threads_update_set_updated_at.sql",
  "./schemas/judgement/triggers/on_judgements_status_changed.sql",

  # Task
  "./schemas/task/triggers/on_tasks_update_set_updated_at.sql",
  "./schemas/task/triggers/on_all_judgements_confirmed_close_task.sql",

  # Evidence
  "./schemas/evidence/triggers/on_task_evidences_insert_validate_due_date.sql",
  "./schemas/evidence/triggers/on_task_evidences_update_validate_due_date.sql",
  "./schemas/evidence/triggers/on_task_evidences_update_set_updated_at.sql",
  "./schemas/evidence/triggers/on_task_evidences_upserted_notify_referee.sql",

  # Matching
  "./schemas/matching/triggers/on_task_referee_requests_update_set_updated_at.sql",
  "./schemas/matching/triggers/on_task_referee_requests_insert_process_matching.sql",
  "./schemas/matching/triggers/on_task_referee_requests_update_process_matching.sql",
  "./schemas/matching/triggers/on_referee_available_time_slots_update_set_updated_at.sql",
  "./schemas/matching/triggers/on_matching_config_update_set_updated_at.sql",

  # Point
  "./schemas/point/triggers/on_point_wallets_update_set_updated_at.sql",

  # Reward
  "./schemas/reward/triggers/on_reward_wallets_update_set_updated_at.sql",
  "./schemas/reward/triggers/on_reward_payouts_update_set_updated_at.sql",

  # Subscription
  "./schemas/subscription/triggers/on_user_subscriptions_update_set_updated_at.sql",

  # Notification
  "./schemas/notification/triggers/on_user_fcm_tokens_update_set_updated_at.sql",

  # Profile
  "./schemas/profile/triggers/on_profiles_update_set_updated_at.sql",

  # Rating
  "./schemas/rating/triggers/on_rating_histories_change_update_user_ratings.sql",
  "./schemas/rating/triggers/on_user_ratings_update_set_updated_at.sql",

  # Stripe
  "./schemas/stripe/triggers/on_stripe_accounts_update_set_updated_at.sql",

# policies
  "./schemas/judgement/policies/judgement_threads_policies.sql",
  "./schemas/judgement/policies/judgement_thread_assets_policies.sql",
  "./schemas/task/policies/tasks_policies.sql",
  "./schemas/evidence/policies/task_evidences_policies.sql",
  "./schemas/evidence/policies/task_evidence_assets_policies.sql",
  "./schemas/matching/policies/task_referee_requests_policies.sql",
  "./schemas/matching/policies/referee_available_time_slots_policies.sql",
  "./schemas/rating/policies/rating_histories_policies.sql",
  "./schemas/rating/policies/user_ratings_policies.sql",
  "./schemas/profile/policies/profiles_policies.sql",
  "./schemas/stripe/policies/stripe_accounts_policies.sql",
  "./schemas/judgement/policies/judgements_policies.sql",
  "./schemas/subscription/policies/subscription_plans_policies.sql",
  "./schemas/subscription/policies/subscription_plan_prices_policies.sql",
  "./schemas/subscription/policies/user_subscriptions_policies.sql",
  "./schemas/point/policies/point_wallets_policies.sql",
  "./schemas/point/policies/point_ledger_policies.sql",
  "./schemas/reward/policies/reward_wallets_policies.sql",
  "./schemas/reward/policies/reward_ledger_policies.sql",
  "./schemas/reward/policies/reward_payouts_policies.sql",
  "./schemas/notification/policies/user_fcm_tokens_policies.sql",

]

[db.seed]
# If enabled, seeds the database after migrations during a db reset.
enabled = true
# Specifies an ordered list of seed files to load during db reset.
# Supports glob patterns relative to supabase directory: "./seeds/*.sql"
sql_paths = ["./seed.sql"]

[realtime]
enabled = true
# Bind realtime via either IPv4 or IPv6. (default: IPv4)
# ip_version = "IPv6"
# The maximum length in bytes of HTTP request headers. (default: 4096)
# max_header_length = 4096

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://127.0.0.1"
# OpenAI API Key to use for Supabase AI in the Supabase Studio.
openai_api_key = "env(OPENAI_API_KEY)"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326
# admin_email = "admin@email.com"
# sender_name = "Admin"

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

# Image transformation API is available to Supabase Pro plan.
# [storage.image_transformation]
# enabled = true

# Uncomment to configure local storage buckets
# [storage.buckets.images]
# public = false
# file_size_limit = "50MiB"
# allowed_mime_types = ["image/png", "image/jpeg"]
# objects_path = "./images"

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = [
  "http://localhost:3000/**", # Required for Next.js Auth Callback (Local & Emulator)
  "https://peppercheck.dev/**"  # Required for Production
]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true
# Allow/disallow anonymous sign-ins to your project.
enable_anonymous_sign_ins = false
# Allow/disallow testing manual linking of accounts
enable_manual_linking = false
# Passwords shorter than this value will be rejected as weak. Minimum 6, recommended 8 or more.
minimum_password_length = 6
# Passwords that do not meet the following requirements will be rejected as weak. Supported values
# are: `letters_digits`, `lower_upper_letters_digits`, `lower_upper_letters_digits_symbols`
password_requirements = ""

[auth.rate_limit]
# Number of emails that can be sent per hour. Requires auth.email.smtp to be enabled.
email_sent = 2
# Number of SMS messages that can be sent per hour. Requires auth.sms to be enabled.
sms_sent = 30
# Number of anonymous sign-ins that can be made per hour per IP address. Requires enable_anonymous_sign_ins = true.
anonymous_users = 30
# Number of sessions that can be refreshed in a 5 minute interval per IP address.
token_refresh = 150
# Number of sign up and sign-in requests that can be made in a 5 minute interval per IP address (excludes anonymous users).
sign_in_sign_ups = 30
# Number of OTP / Magic link verifications that can be made in a 5 minute interval per IP address.
token_verifications = 30

# Configure one of the supported captcha providers: `hcaptcha`, `turnstile`.
# [auth.captcha]
# enabled = true
# provider = "hcaptcha"
# secret = ""

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false
# If enabled, users will need to reauthenticate or have logged in recently to change their password.
secure_password_change = false
# Controls the minimum amount of time that must pass before sending another signup confirmation or password reset email.
max_frequency = "1s"
# Number of characters used in the email OTP.
otp_length = 6
# Number of seconds before the email OTP expires (defaults to 1 hour).
otp_expiry = 3600

# Use a production-ready SMTP server
# [auth.email.smtp]
# enabled = true
# host = "smtp.sendgrid.net"
# port = 587
# user = "apikey"
# pass = "env(SENDGRID_API_KEY)"
# admin_email = "admin@email.com"
# sender_name = "Admin"

# Uncomment to customize email template
# [auth.email.template.invite]
# subject = "You have been invited"
# content_path = "./supabase/templates/invite.html"

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = false
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false
# Template for sending OTP to users
template = "Your code is {{ .Code }}"
# Controls the minimum amount of time that must pass before sending another sms otp.
max_frequency = "5s"

# Use pre-defined map of phone number to OTP for testing.
# [auth.sms.test_otp]
# 4152127777 = "123456"

# Configure logged in session timeouts.
# [auth.sessions]
# Force log out after the specified duration.
# timebox = "24h"
# Force log out if the user has been inactive longer than the specified duration.
# inactivity_timeout = "8h"

# This hook runs before a token is issued and allows you to add additional claims based on the authentication method used.
# [auth.hook.custom_access_token]
# enabled = true
# uri = "pg-functions://<database>/<schema>/<hook_name>"

# Configure one of the supported SMS providers: `twilio`, `twilio_verify`, `messagebird`, `textlocal`, `vonage`.
[auth.sms.twilio]
enabled = false
account_sid = ""
message_service_sid = ""
# DO NOT commit your Twilio auth token to git. Use environment variable substitution instead:
auth_token = "env(SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN)"

# Multi-factor-authentication is available to Supabase Pro plan.
[auth.mfa]
# Control how many MFA factors can be enrolled at once per user.
max_enrolled_factors = 10

# Control MFA via App Authenticator (TOTP)
[auth.mfa.totp]
enroll_enabled = false
verify_enabled = false

# Configure MFA via Phone Messaging
[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false
otp_length = 6
template = "Your code is {{ .Code }}"
max_frequency = "5s"

# Configure MFA via WebAuthn
# [auth.mfa.web_authn]
# enroll_enabled = true
# verify_enabled = true

# Use an external OAuth provider. The full list of providers are: `apple`, `azure`, `bitbucket`,
# `discord`, `facebook`, `github`, `gitlab`, `google`, `keycloak`, `linkedin_oidc`, `notion`, `twitch`,
# `twitter`, `slack`, `spotify`, `workos`, `zoom`.
[auth.external.apple]
enabled = false
client_id = ""
# DO NOT commit your OAuth provider secret to git. Use environment variable substitution instead:
secret = "env(SUPABASE_AUTH_EXTERNAL_APPLE_SECRET)"
# Overrides the default auth redirectUrl.
redirect_uri = ""
# Overrides the default auth provider URL. Used to support self-hosted gitlab, single-tenant Azure,
# or any other third-party OIDC providers.
url = ""
# If enabled, the nonce check will be skipped. Required for local sign in with Google auth.
skip_nonce_check = false

[auth.external.google]
enabled = true
client_id = "env(GOOGLE_CLIENT_ID)"
secret = "env(GOOGLE_CLIENT_SECRET)"
skip_nonce_check = true

# Use Firebase Auth as a third-party provider alongside Supabase Auth.
[auth.third_party.firebase]
enabled = false
# project_id = "my-firebase-project"

# Use Auth0 as a third-party provider alongside Supabase Auth.
[auth.third_party.auth0]
enabled = false
# tenant = "my-auth0-tenant"
# tenant_region = "us"

# Use AWS Cognito (Amplify) as a third-party provider alongside Supabase Auth.
[auth.third_party.aws_cognito]
enabled = false
# user_pool_id = "my-user-pool-id"
# user_pool_region = "us-east-1"

# Use Clerk as a third-party provider alongside Supabase Auth.
[auth.third_party.clerk]
enabled = false
# Obtain from https://clerk.com/setup/supabase
# domain = "example.clerk.accounts.dev"

[edge_runtime]
enabled = true
# Configure one of the supported request policies: `oneshot`, `per_worker`.
# Use `oneshot` for hot reload, or `per_worker` for load testing.
policy = "oneshot"
# Port to attach the Chrome inspector for debugging edge functions.
inspector_port = 8083
# The Deno major version to use.
deno_version = 1

# [edge_runtime.secrets]
# secret_key = "env(SECRET_VALUE)"

[analytics]
enabled = true
port = 54327
# Configure one of the supported backends: `postgres`, `bigquery`.
backend = "postgres"

# Experimental features may be deprecated any time
[experimental]
# Configures Postgres storage engine to use OrioleDB (S3)
orioledb_version = ""
# Configures S3 bucket URL, eg. <bucket_name>.s3-<region>.amazonaws.com
s3_host = "env(S3_HOST)"
# Configures S3 bucket region, eg. us-east-1
s3_region = "env(S3_REGION)"
# Configures AWS_ACCESS_KEY_ID for S3 bucket
s3_access_key = "env(S3_ACCESS_KEY)"
# Configures AWS_SECRET_ACCESS_KEY for S3 bucket
s3_secret_key = "env(S3_SECRET_KEY)"

[functions.generate-upload-url]
enabled = true
verify_jwt = false
import_map = "./functions/generate-upload-url/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/generate-upload-url/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/generate-upload-url/*.html" ]

[functions.billing-setup]
enabled = true
verify_jwt = true
import_map = "./functions/billing-setup/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/billing-setup/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/billing-setup/*.html" ]



[functions.payout-setup]
enabled = true
verify_jwt = true
import_map = "./functions/payout-setup/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/payout-setup/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/payout-setup/*.html" ]









[functions.create-stripe-checkout]
enabled = true
verify_jwt = true
import_map = "./functions/create-stripe-checkout/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/create-stripe-checkout/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/create-stripe-checkout/*.html" ]

[functions.handle-stripe-webhook]
enabled = true
verify_jwt = false
import_map = "./functions/handle-stripe-webhook/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/handle-stripe-webhook/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/handle-stripe-webhook/*.html" ]

[functions.verify-google-purchase]
enabled = true
verify_jwt = true
import_map = "./functions/verify-google-purchase/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/verify-google-purchase/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/verify-google-purchase/*.html" ]

[functions.send-notification]
enabled = true
verify_jwt = false  # TODO: enable after supabase cli bug fixed
import_map = "./functions/send-notification/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/send-notification/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/send-notification/*.html" ]

[functions.execute-pending-payouts]
enabled = true
verify_jwt = false
import_map = "./functions/execute-pending-payouts/deno.json"
